# `ca65`

<!-- Generated by `cargo xtask docs` from the assembler's own declared surface. Edits here are overwritten; change the declaration instead. -->

NES 6502, ca65 syntax (assemble + link).

`--dialect` also accepts `nes`.

## Directives

32 directives, 121 spellings. This is the list the parser looks a word up in, so a spelling here is one the assembler accepts and a spelling missing from it is one it refuses.

| Directive | Spellings | Notes |
|---|---|---|
| `align` | `.align` |  |
| `asciiz` | `.asciiz` |  |
| `assert` | `.assert` |  |
| `autoimport` | `.autoimport` | Accepted and discarded — it changes no bytes |
| `bytes` | `.byte`, `.byt` |  |
| `conditional` | `.if`, `.ifdef`, `.ifndef`, `.elseif`, `.else`, `.endif` |  |
| `dbyt` | `.dbyt` |  |
| `dword` | `.dword` |  |
| `endmacro` | `.endmacro`, `.endmac` |  |
| `export` | `.export`, `.exportzp` |  |
| `forceimport` | `.forceimport` | Refused, as the reference refuses it — only usable when a linker resolves it from another module, and asm198x emits a binary |
| `global` | `.global`, `.globalzp` |  |
| `import` | `.import`, `.importzp` |  |
| `incbin` | `.incbin` |  |
| `include` | `.include` |  |
| `local` | `.local` |  |
| `macro` | `.macro`, `.mac` |  |
| `message` | `.warning`, `.error`, `.fatal` |  |
| `out` | `.out` |  |
| `repeat` | `.repeat`, `.endrepeat` |  |
| `res` | `.res` |  |
| `segment` | `.segment` |  |
| `segment-shorthand` | `.code`, `.data`, `.bss`, `.rodata`, `.zeropage`, `.pushseg`, `.popseg` |  |
| `unsupported-conditionals` | `.ifblank`, `.ifconst`, `.ifnblank`, `.ifnconst`, `.ifnref`, `.ifp02`, `.ifp4510`, `.ifp816`, `.ifpc02`, `.ifpsc02`, `.ifref` | Recognised, and not implemented |
| `unsupported-cpu` | `.setcpu`, `.pushcpu`, `.popcpu`, `.smart`, `.p02`, `.p4510`, `.p816`, `.pc02`, `.psc02` | Recognised, and not implemented |
| `unsupported-diagnostics` | `.list`, `.listbytes`, `.pagelen`, `.pagelength`, `.linecont`, `.fileopt`, `.fopt`, `.dbg`, `.debuginfo`, `.case`, `.charmap`, `.localchar`, `.feature`, `.null` | Recognised, and not implemented |
| `unsupported-everything` | `.bankbytes`, `.bitand`, `.bitnot`, `.bitor`, `.bitxor`, `.faraddr`, `.hibytes`, `.lobytes`, `.mod`, `.not`, `.or`, `.shl`, `.shr`, `.xor` | Recognised, and not implemented |
| `unsupported-macros` | `.define`, `.delmac`, `.delmacro`, `.macpack`, `.undef`, `.undefine`, `.ident`, `.concat`, `.sprintf`, `.string`, `.left`, `.mid`, `.right` | Recognised, and not implemented |
| `unsupported-scopes` | `.proc`, `.endproc`, `.scope`, `.endscope`, `.struct`, `.union`, `.enum`, `.tag`, `.end` | Recognised, and not implemented |
| `unsupported-segments` | `.org`, `.reloc` | Recognised, and not implemented |
| `unsupported-symbol` | `.condes`, `.constructor`, `.destructor`, `.interruptor` | Recognised, and not implemented |
| `words` | `.word`, `.addr` |  |

Instruction mnemonics are not listed here — they come from the instruction-set spec and have their own reference.

## Projects in more than one file

A relative request is looked for in **the including file's directory, then each enclosing includer's**, then in the `-I` search directories in the order they were given.

The anchor is pinned against this dialect's own reference assembler, and the dialects genuinely disagree — [Projects in more than one file](../../guide/multi-file.md) has the comparison.

## Elsewhere

- [Dialects](../dialects.md) — every dialect and what each is the syntax of
- [Where we differ](../../divergences.md) — every known difference from a reference assembler
- [Moving a project across](../../migrate.md) — adopting this on a project that already builds
