/target
**/*.rs.bk
*.bin
# ...but binary fixture assets are part of the committed corpus
!crates/asm198x/tests/fixtures/**/*.bin
a.out
.DS_Store

# Vale synced package
.vale/

# mdBook's build output. mdBook is withdrawn, but a stale local `book/` from
# before the change should still never be committed.
/docs/book/book/
